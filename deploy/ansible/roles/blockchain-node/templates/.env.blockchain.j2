# Autogenerated by Ansible - P2P/security env for XAI node
XAI_NETWORK={{ network_name | default('testnet') }}
XAI_PEER_REQUIRE_CLIENT_CERT={{ (p2p_require_client_cert | default(false)) | ternary('1','0') }}
XAI_TRUSTED_PEER_CERT_FPS={{ (p2p_trusted_cert_fps | default([])) | join(',') }}
XAI_TRUSTED_PEER_PUBKEYS={{ (p2p_trusted_peer_pubkeys | default([])) | join(',') }}
XAI_PEER_NONCE_TTL_SECONDS={{ p2p_nonce_ttl_seconds | default(180) }}
XAI_PEER_CA_BUNDLE={{ p2p_ca_bundle | default('') }}
XAI_TRUSTED_PEER_CERT_FPS_FILE={{ p2p_trusted_cert_fps_file | default('') }}
XAI_TRUSTED_PEER_PUBKEYS_FILE={{ p2p_trusted_peer_pubkeys_file | default('') }}
XAI_P2P_DNS_SEEDS={{ p2p_dns_seeds | default([]) | join(',') }}
XAI_P2P_BOOTSTRAP_NODES={{ p2p_bootstrap_nodes | default([]) | join(',') }}
