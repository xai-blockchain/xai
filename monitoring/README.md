# Monitoring

Prometheus and Alertmanager configuration files live in this directory.

- `prometheus_alerts.yml`
- `alertmanager.yml`
- `alert_templates.tmpl`

If you run the monitoring stack via Docker, review `docker/monitoring/` for compose files and service ports.
