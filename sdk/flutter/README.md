# Flutter SDK

Flutter/Dart SDK for the XAI node API.

## Install (local)

Add to your `pubspec.yaml`:

```yaml
dependencies:
  xai_sdk:
    path: ../sdk/flutter
```

Then run:

```bash
flutter pub get
```

See `lib/` for available APIs.
