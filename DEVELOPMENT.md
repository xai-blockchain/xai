# Development

## Reproducible builds
- Build from a clean checkout with no untracked build outputs.
- Pin toolchains and dependencies (lockfiles, toolchain files, or version managers).
- Prefer deterministic build settings; avoid embedding timestamps or host-specific paths.
