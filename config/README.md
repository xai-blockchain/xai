# Configuration Files

- `cors.yaml`: CORS origin allowlist for the node API.
- `p2p_versions.yaml`: P2P protocol version metadata.

Most runtime configuration is controlled by environment variables in `src/xai/core/config.py`.
