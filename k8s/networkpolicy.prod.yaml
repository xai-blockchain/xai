apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: xai-blockchain-p2p-restrict-prod
  namespace: xai-blockchain
  labels:
    app: xai-blockchain
spec:
  podSelector:
    matchLabels:
      app: xai-blockchain
      component: node
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - ipBlock:
            cidr: 203.0.113.0/24  # Replace with production validator CIDR(s)
      ports:
        - port: 8545
          protocol: TCP
        - port: 8546
          protocol: TCP
        - port: 8547
          protocol: TCP
        - port: 9090
          protocol: TCP
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 443
          protocol: TCP
        - port: 80
          protocol: TCP
