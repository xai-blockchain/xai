# IMPORTANT: Replace placeholder values with real secrets in production
# Use a secrets management solution (Vault, AWS Secrets Manager, etc.)
apiVersion: v1
kind: Secret
metadata:
  name: xai-secrets
type: Opaque
stringData:
  VALIDATOR_KEY: "change-me-validator-key"
  NODE_KEY: "change-me-node-key"
  JWT_SECRET: "change-me-jwt-secret"
