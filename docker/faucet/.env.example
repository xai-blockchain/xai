# XAI Testnet Faucet Configuration
# Copy this file to .env and adjust values as needed

# XAI Node API URL
# Use host.docker.internal for local node, or specify remote node URL
XAI_API_URL=http://host.docker.internal:18545
XAI_FAUCET_PATH=/api/faucet
XAI_TRANSFER_PATH=/transfer
FAUCET_SENDER=

# Faucet web UI port
FAUCET_PORT=8086

# Amount to dispense per request (XAI)
FAUCET_AMOUNT=100
# Optional recipient balance cap (0 disables)
FAUCET_MAX_BALANCE=0

# Cooldown period in seconds
# Default: 3600 (1 hour)
FAUCET_COOLDOWN=3600

# Environment / network safety
FAUCET_ENV=production
XAI_NETWORK=testnet
FAUCET_ALLOW_MAINNET=0
REQUIRE_CAPTCHA_IN_PROD=1
# Require captcha even outside production (leave empty to use production default)
FAUCET_REQUIRE_CAPTCHA=

# IP rate limiting (additional anti-abuse)
FAUCET_IP_COOLDOWN=600
FAUCET_IP_MAX_PER_WINDOW=5
FAUCET_IP_WINDOW=3600

# Access control (devnet)
# Optional allowlists (comma-separated). If set, only these IPs/addresses can request.
FAUCET_ALLOWED_IPS=
FAUCET_ALLOWED_ADDRESSES=

# Address prefix validation (comma-separated)
FAUCET_ALLOWED_PREFIXES=TXAI

# hCaptcha (optional but recommended in production)
HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET_KEY=

# Redis (optional but recommended for production)
REDIS_URL=redis://redis:6379/0
