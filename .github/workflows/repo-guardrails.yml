name: Repo guardrails

on:
  push:
  pull_request:

jobs:
  guardrails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Repo sanity checks
        run: ./scripts/ci/check-repo-sanity.sh
      - name: Secret scan (gitleaks)
        uses: gitleaks/gitleaks-action@v2
